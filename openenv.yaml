name: bug-triage-env
version: 1.0
description: Bug report triage RL environment
actions:
  priority: string
  labels: list
  assigned_team: string
  milestone: string
  reasoning: string
observations:
  bug_report: object
  task_id: string
  score: float
  reward: float
  feedback: string
  done: bool
tasks:
  - easy
  - medium
  - hard
endpoints:
  reset: /reset
  step: /step
  state: /state